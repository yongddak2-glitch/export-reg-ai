# 실행 검증 기록

검증일: 2026-09-13. 합성자료 및 Mock 모드 기준입니다.

## 판정

**요청한 핵심 흐름은 Mock 모드에서 실행 검증을 통과했습니다.**

PDF 업로드 → 12개 항목 CHECK → 모델/버전/누락/판독 불가 구분 → TO DO → 사실 확인 입력 → PREPARE 초안과 PDF 생성 → 수정 PDF 재업로드 → Before/After 비교가 작동했습니다.

실제 API 키가 없어 **OpenAI의 실제 의미 매칭 결과와 실무 정확도는 미검증**입니다. GitHub 저장소 생성·푸시·Cloud 원격 배포는 수행하지 않았습니다. 재현 및 배포 방법은 README에 있습니다.

## 환경

- Python 3.12.14
- Streamlit 1.63.0
- OpenAI Python SDK 3.13.0
- Pydantic 2.13.5
- PyMuPDF 1.26.6
- ReportLab 4.4.9
- python-dotenv 1.2.2
- `pip install -r requirements.txt` 성공
- `pip check`: No broken requirements found
- Streamlit 실제 프로세스 시작 후 health endpoint: **200 / ok**
- 앱 HTML 응답: **200**

## 자동 테스트

명령: `python -m pytest tests -q`

결과: **11 passed**.

| 테스트 | 확인한 내용 |
|---|---|
| 초기 근거/Gap | 초기 12개 상태와 페이지 원문 일치, 전문가 검토 상태, 다음 작업 |
| PREPARE / PDF 재입력 | 확인 체크 없으면 거부, 임의 값 없음, 확인 사실 PDF 보존, 재분석 변화 |
| 재검토 | 유지보수·도면 보완, 설치 설명 유실에 따른 재검토, 모델 불일치 유지 |
| 허위 인용·ID | 원문에 없는 인용은 제외·보류, 잘못된 요구사항 ID는 분석 거부 |
| 상태 스키마·판정 경계 | 금지 상태 거부, AI의 사용자 확인 상태 강제 해제, 승인 표현 차단 |
| 업로드 예외 | 파일 없음·손상·중복·크기 초과·암호화·페이지 초과 거부 |
| 긴 한국어 초안 | 줄바꿈·PDF 내보내기 후 입력 사실이 누락되지 않음 |
| SDK 구조화 응답 | 실제 SDK가 가짜 HTTP 응답을 Pydantic으로 파싱, strict JSON Schema·store=False 확인 |
| API 실패 처리 | 오류를 Mock 성공으로 바꾸지 않고 민감한 원문 오류 메시지 노출 방지 |
| API PREPARE 계약 | AI가 기존 사실 ID만 선택·배치하고 본문에 새로운 값 추가 불가 |
| Streamlit UI 통합 | 파일 업로더에 6개 PDF 바이트 입력, Analyze, 12행 Matrix, 필터, 질문 입력, 초안, 2개 수정 PDF 업로드, RE-CHECK 표시 |

UI 테스트는 Streamlit 공식 `AppTest`의 FileUploader·버튼·텍스트 입력·체크박스·표를 사용했습니다. 파일 대신 결과를 주입하거나 분석 결과를 미리 만들어 UI에 넣지 않았습니다.

## UI 비교에서 확인한 구체적 결과

- 초기: 문서 Gap **6개**, 근거 후보가 있는 항목 **10개**, 전문가 판단 항목 **4개**.
- BC-90 시험자료 → `MODEL_MISMATCH`.
- 설계 A 도면 → `VERSION_MISMATCH`.
- 유지보수 주기 → `INSUFFICIENT_INFORMATION` 및 주기 확인 질문.
- 취급·운반 → `NOT_FOUND_IN_UPLOAD`.
- 이미지 PDF 소음 기록 → `UNREADABLE`.
- PREPARE에 입력한 합성 점검주기가 초안에 그대로 포함됨.
- 수정 매뉴얼/도면 업로드 후 유지보수·도면·방호 설명은 문서 Gap 보완 후보 **3개**. 방호 설명의 전문가 판단은 유지됨.
- 수정 매뉴얼에서 설치 설명이 사라져 REQ-04가 `EVIDENCE_FOUND → NOT_FOUND_IN_UPLOAD`, **재검토 필요**로 표시됨.
- 입력/분석이 바뀌면 이전 보완 초안이 그대로 남지 않음.

## PDF 확인과 수정한 오류

초기 합성 PDF 6개, 수정 PDF 2개, 시험 중 생성한 한국어 초안 PDF를 Poppler로 렌더링해 총 11페이지를 확인했습니다. 긴 초안의 글자가 잘리는 오류를 발견했고 글리프 폭 측정·폰트 임베딩·페이지 분할로 수정했습니다. 긴 한국어 및 영문 원문 보존은 추출 텍스트 테스트로도 확인했습니다.

## 미검증 범위

- 원격 브라우저의 로컬 주소 접속이 `ERR_BLOCKED_BY_CLIENT`로 차단되어 **브라우저 화면의 시각/반응형 검증과 실제 브라우저 파일 선택·다운로드는 수행하지 못했습니다.** 공식 UI 테스트와 로컬 서버 응답을 확인한 범위까지만 주장합니다.
- OpenAI 실제 호출 0회. SDK 계약 테스트의 출력은 가짜 HTTP 응답이며 모델 성능의 증거가 아닙니다.
- 실제 기업/전문가 인터뷰, 실제 기술자료, 전문가의 검토시간, 정확도 비교, 절감률, 지불 의사 실험을 수행하지 않았습니다.
- 12개 범위의 법적 완결성·제품별 적용성·공학적 충분성 검증을 하지 않았습니다.
- OCR·도면 형상 해석·기밀자료 운영·공개 API 앱 비용 통제·프로덕션 보안은 범위 밖입니다.

발표에서 가능한 표현: “합성자료를 활용한 문서 준비 흐름을 구현했고, Mock UI 통합 테스트를 통과했습니다. 실제 AI 성능은 다음 검증 대상입니다.”

현재 해서는 안 되는 표현: “실제 AI가 모든 오류를 찾아냈다”, “CE 판정을 자동화했다”, “실제 기업 업무시간을 특정 비율 줄였다”, “기업 기밀자료에 안전하게 사용할 수 있다.”
